defaults:
  - storm_dmc_transformer_vision
  - _self_

logdir: logdir/storm_dmc_vision/${scenario.name}/sliding_window/seed_${seed}/${now:%Y-%m-%d}/${now:%H-%M-%S}

storm_model:
  sequence_core: sliding_window
