defaults:
  - dmc_experiment
  - model: size_small
  - _self_

logdir: logdir/dmc_expert/${scenario.name}/${model.rssm.core}/seed_${seed}/${now:%Y-%m-%d}/${now:%H-%M-%S}

training:
  expert:
    enabled: true
    # Online evaluation selects this family's final checkpoint.
    eval_every: 0

model:
  hidden: 713
