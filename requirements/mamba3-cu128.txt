# Mamba3 runtime tested with PyTorch 2.8.0 and CUDA 12.8.
# Keep these pins together: CUTLASS, TVM FFI, TileLang, and Quack share private APIs.
# Mamba itself is installed from its pinned source commit with --no-deps by setup_dmc.sh.
# Triton 3.5 overrides Torch 2.8's 3.4 pin because Mamba3 needs small matrix dots.
apache-tvm-ffi==0.1.11
cuda-bindings==13.3.1
cuda-core==1.0.0
cuda-pathfinder==1.5.5
cuda-python==13.3.1
einops==0.8.1
# TileLang leaves this unbounded, but 0.6 requires NumPy 2.
ml-dtypes==0.5.4
nvidia-cutlass-dsl==4.5.2
nvidia-cutlass-dsl-libs-base==4.5.2
quack-kernels==0.5.3
tilelang==0.1.8
torch-c-dlpack-ext==0.1.5
transformers==4.57.6
triton==3.5.0
z3-solver==4.15.4.0
